# User-specific install config — wizard-written, never committed.
config/config.yaml

# API keys for keyed tool backends — never committed (names documented in .env.example).
.env

# Runtime logs — one file per day, never committed.
logs/

# Downloaded TTS voice models — fetched by piper's downloader, never committed.
models/voices/

# Locally-extracted PulseAudio client libs (no-root WSL bootstrap) — see interface/audio.py.
vendor/

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/

# Virtual environment
.venv/
venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo
