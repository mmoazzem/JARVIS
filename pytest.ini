[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    integration: live tests against a running Ollama daemon (opt-in: pytest -m integration / make test-live)
; Bare `pytest` runs only the fast, mocked unit tier — integration is opt-in.
addopts = -m "not integration"
