# LLM runtime
ollama>=0.2.0
openai>=1.0.0          # Ollama uses OpenAI-compatible API

# Config
pyyaml>=6.0

# HTTP client
httpx>=0.27.0

# Data validation
pydantic>=2.0.0

# Terminal output
rich>=13.0.0

# Terminal input: bracketed paste so multi-line pastes arrive as one message
prompt_toolkit>=3.0.0

# System scan (CPU/RAM/process info)
psutil>=5.9.0

# Voice output — piper-tts (piper1-gpl, GPL license: known, deliberate choice).
# CPU inference; the GPU belongs to the chat model.
piper-tts>=1.4,<2

# Credentials — .env loading for keyed tool backends, read at boot
python-dotenv>=1.0.0

# Web search — keyless DuckDuckGo backend behind BaseSearch
ddgs>=9.0

# Fetch-and-read — main-content extraction from fetched pages (BaseExtractor)
trafilatura>=2.0

# Frontend seam — WebSocket server bridging respond() to browser clients
websockets>=16.0

